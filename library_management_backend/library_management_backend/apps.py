from django.apps import AppConfig

class LibraryManagementBackendConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'library_management_backend'  # Ensure this matches your folder name
